###################################################### -*- mode: r -*- #####
## Scenario setup for Iterated Race (iRace).
############################################################################
## To use the default value of a parameter of iRace, simply do not set
## the parameter (comment it out in this file, and do not give any
## value on the command line).

## File that contains the description of the parameters.
parameterFile = "./parameters-SA.txt"

## Directory where the programs will be run.
execDir = "/data/gent/vo/001/gvo00147/vsc41980/iRR_new"

## Directory where tuning instances are located, either absolute path or
## relative to current directory.
# Do a final test on the same training/test instances... That gives us the best one overall
trainInstancesDir = "./Instances/"
testInstancesDir = "./Instances/"

# Test the final 6 elite configurations on all test instances
# We will pick the elite config that has the best average performance on the test instances
testNbElites = 6

## Parallelization: launch in parallel up to N calls of the target algorithm
## We set this value to 8 since we have 8 CPU's, and each target run only
## requires one thread.
## Slurm: no limits
## parallel = 2

## The maximum number of runs (invocations of targetRunner) that will performed. It
## determines the (maximum) budget of experiments for the tuning.
## Each experiments takes ~600 sec: Time (s) ≃ maxExperiments/parallel*600
maxExperiments = 1000

## Enable or disable the sampling of training instances. 
sampleInstances = 1

## A value of 0 silences all debug messages. Higher values provide
## more verbose debug messages.
# debugLevel = 0

## Set link to forbidden configurations
#forbiddenFile = "./forbidden.txt"

## Set link to default configuration
#configurationsFile = "./default.txt"

# Weights should never sum up to more than one
#repairConfiguration = function (configuration, parameters, digits)
#{
#isOperator <- parameters$name[colnames(configuration)] %in% c("kick_rate","kempe_rate","shake_rate", "beam_rate", "component_rate")
#configuration[isOperator] <- configuration[isOperator] / sum(configuration[isOperator])
#configuration[isOperator] <- round(configuration[isOperator], digits)
#return(configuration)
#}

## END of scenario file
############################################################################
